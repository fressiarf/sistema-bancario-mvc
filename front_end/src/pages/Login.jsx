import React from "react";
import FormLogin from "../components/login/FormLogin";

const Login = () => {
    return (
        <div>
            <FormLogin />
        </div>
    );
};

export default Login;
